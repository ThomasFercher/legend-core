import 'package:flutter/widgets.dart';

class LegendLogo extends StatelessWidget {
  const LegendLogo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
